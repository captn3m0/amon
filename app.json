{
  "name": "amon",
  "description": "amon is a hacking tool for maintaining access to accounts using oauth tokens.",
  "keywords": [
    "hack",
    "amon",
    "oauth",
    "slack"
  ],
  "website": "https://github.com/captn3m0/amon",
  "repository": "https://github.com/captn3m0/amon",
  "logo": "https://rawgit.com/captn3m0/amon/master/app/assets/images/logo.png",
  "success_url": "/",
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate"
  },
  "env": {
    "SECRET_KEY_BASE": {
      "description": "A secret key for verifying the integrity of signed cookies.",
      "generator": "secret"
    },
    "SLACK_ID": {
      "description": "The application key for slack. Get one from https://api.slack.com/applications/new",
      "required": true
    },
    "SLACK_SECRET": {
      "description": "The application secret for slack. Set the redirect_uri in Slack to https://APP-NAME.herokuapp.com/auth/slack/callback, where APP-NAME is what you've filled above",
      "required": true
    },
    "RAILS_ENV": "production",
    "RACK_ENV": "production"
  },
  "addons": [
    "heroku-postgresql:hobby-dev"
  ]
}